name: Docker Image CI EnvDevJS 7.4 FPM Alpine

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
      - name: Build the Docker image EnvDevJS 7.4 FPM Alpine
        run: docker build . --file Dockerfile --tag vfac/envdevjs:7.4-fpm-alpine
